import 'package:flutter/material.dart';

class question_write extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("질문 작성"),
      ),
    );
  }
}
